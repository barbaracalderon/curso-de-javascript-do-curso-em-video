// Repetição com teste no início (WHILE)
var c = 1
while (c <= 6) {
    console.log(`Passo ${c}`)
    c++
}

// Repetição com teste no final (DO WHILE)
var d = 1
do {
    console.log(`Passo ${d}`)
    c++
} while (c <= 6)